parametrize the login functionality by using dataprovider and testNG.

Perfect 👍 — this is a **very common real-time interview question**:
👉 *“How do you implement Data-Driven Testing using Excel in TestNG?”*

Let’s explain it **step-by-step with clear flow + code** 👇

---

# ✅ 🔥 Overall Approach

> We read test data from Excel using Apache POI and pass it to TestNG test methods using `@DataProvider`.

---

# 🧠 Flow Diagram

```
Excel File → Apache POI → DataProvider → TestNG Test Method → Execution
```

---

# ✅ Step 1: Add Dependencies

### Maven dependency (Apache POI)

```xml
<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi</artifactId>
    <version>5.2.3</version>
</dependency>

<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi-ooxml</artifactId>
    <version>5.2.3</version>
</dependency>
```

---

# ✅ Step 2: Excel File Example

| Username | Password |
| -------- | -------- |
| user1    | pass1    |
| user2    | pass2    |

👉 File: `login.xlsx`

---

# ✅ Step 3: Create Utility to Read Excel

```java
import java.io.FileInputStream;
import org.apache.poi.xssf.usermodel.*;

public class ExcelUtil {

    public static Object[][] getData(String filePath, String sheetName) throws Exception {

        FileInputStream fis = new FileInputStream(filePath);
        XSSFWorkbook workbook = new XSSFWorkbook(fis);
        XSSFSheet sheet = workbook.getSheet(sheetName);

        int rowCount = sheet.getPhysicalNumberOfRows();
        int colCount = sheet.getRow(0).getPhysicalNumberOfCells();

        Object[][] data = new Object[rowCount - 1][colCount];

        for (int i = 1; i < rowCount; i++) {
            for (int j = 0; j < colCount; j++) {
                data[i - 1][j] = sheet.getRow(i).getCell(j).toString();
            }
        }

        workbook.close();
        return data;
    }
}
```

---

# ✅ Step 4: Create DataProvider

```java
import org.testng.annotations.DataProvider;

public class DataProviderClass {

    @DataProvider(name = "loginData")
    public Object[][] loginData() throws Exception {
        return ExcelUtil.getData("login.xlsx", "Sheet1");
    }
}
```

---

# ✅ Step 5: Use in TestNG Test

```java
import org.openqa.selenium.*;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.*;

public class LoginTest {

    WebDriver driver;

    @BeforeMethod
    public void setup() {
        driver = new ChromeDriver();
        driver.get("https://example.com/login");
    }

    @Test(dataProvider = "loginData", dataProviderClass = DataProviderClass.class)
    public void testLogin(String username, String password) {

        driver.findElement(By.id("username")).sendKeys(username);
        driver.findElement(By.id("password")).sendKeys(password);
        driver.findElement(By.id("loginBtn")).click();

        System.out.println("Executed for: " + username);
    }

    @AfterMethod
    public void tearDown() {
        driver.quit();
    }
}
```

---

# 🔥 What Happens Internally

👉 For Excel:

| user1 | pass1 |
| user2 | pass2 |

👉 Test runs:

```
Iteration 1 → user1, pass1
Iteration 2 → user2, pass2
```

---

# 🧠 Key Concepts

| Concept          | Meaning                 |
| ---------------- | ----------------------- |
| Apache POI       | Read Excel              |
| DataProvider     | Supply data to test     |
| Object[][]       | Required format         |
| Parameterization | Run test multiple times |

---

# 💡 Interview Points (VERY IMPORTANT)

Say this 👇:

> I use Apache POI to read Excel data and pass it to TestNG using DataProvider, which allows me to run the same test with multiple data sets without duplicating code.

---

# 🚀 Advanced Points (For Your Experience)

Mention these to impress:

✔ Separate Excel utility class
✔ Handle different data types (String, numeric)
✔ Use config for file path
✔ Use POM for login

---

# ⚠️ Common Mistakes

| Mistake              | Fix            |
| -------------------- | -------------- |
| Hardcoding data      | Use Excel      |
| Not closing workbook | Always close   |
| Wrong data format    | Use Object[][] |

---

# 🎯 Short Answer (Quick)

> I read data from Excel using Apache POI and pass it to TestNG test methods using DataProvider, allowing the same test to run with multiple inputs.

---

If you want next:
✅ Convert this into your **framework structure (like your Selenium project)**
✅ Handle Excel with **multiple sheets + dynamic columns (advanced)**
