parametrize the login functionality by using dataprovider and testNG.

Below is a **step-by-step explanation of reading Excel data using TestNG DataProvider**, the way a **senior automation engineer (10 years experience)** would explain it in an interview — including **framework flow, code design, and execution process**.

---

# 🔥 Goal

We want to:

✅ Read test data from Excel
✅ Pass that data to TestNG test method
✅ Run same test multiple times with different data

---

# 🔥 Example Excel File

Suppose `LoginData.xlsx`

| Username | Password |
| -------- | -------- |
| admin1   | pass1    |
| admin2   | pass2    |
| admin3   | pass3    |

---

# 🔥 Execution Flow

```text id="7ow2xk"
Excel File
   ↓
Excel Utility
   ↓
@DataProvider
   ↓
@Test Method
   ↓
Multiple Test Executions
```

---

# ✅ Step 1: Add Required Dependencies

Maven dependencies:

```xml id="vc3p2y"
<dependency>
    <groupId>org.testng</groupId>
    <artifactId>testng</artifactId>
    <version>7.10.2</version>
</dependency>

<dependency>
    <groupId>org.apache.poi</groupId>
    <artifactId>poi-ooxml</artifactId>
    <version>5.2.5</version>
</dependency>
```

Tools used:

* TestNG
* Apache POI

---

# ✅ Step 2: Create Excel Utility Class

This class reads data from Excel.

---

## ExcelReader.java

```java id="06cqf3"
import java.io.FileInputStream;
import org.apache.poi.ss.usermodel.*;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;

public class ExcelReader {

    public static Object[][] getExcelData(String path, String sheetName) throws Exception {

        FileInputStream fis = new FileInputStream(path);
        Workbook workbook = new XSSFWorkbook(fis);
        Sheet sheet = workbook.getSheet(sheetName);

        int rows = sheet.getPhysicalNumberOfRows();
        int cols = sheet.getRow(0).getPhysicalNumberOfCells();

        Object[][] data = new Object[rows - 1][cols];

        for(int i = 1; i < rows; i++) {
            for(int j = 0; j < cols; j++) {
                data[i - 1][j] = sheet.getRow(i).getCell(j).toString();
            }
        }

        workbook.close();
        fis.close();

        return data;
    }
}
```

---

# 🔥 What this does

Reads:

```text id="bpb2m3"
admin1 pass1
admin2 pass2
admin3 pass3
```

and converts into:

```java id="bkyly0"
Object[][]
```

because TestNG DataProvider requires:

```java id="e0jmlz"
Object[][]
```

---

# ✅ Step 3: Create DataProvider Method

---

## DataProviderClass.java

```java id="3f9e9t"
import org.testng.annotations.DataProvider;

public class DataProviderClass {

    @DataProvider(name = "loginData")
    public Object[][] getData() throws Exception {
        return ExcelReader.getExcelData("LoginData.xlsx", "Sheet1");
    }
}
```

---

# 🔥 Role of DataProvider

TestNG will call:

```java id="zxtw7d"
getData()
```

and receive:

```java id="u6q1ut"
Object[][]
```

---

# ✅ Step 4: Use DataProvider in Test Class

---

## LoginTest.java

```java id="vwuwna"
import org.testng.annotations.Test;

public class LoginTest {

    @Test(dataProvider = "loginData", dataProviderClass = DataProviderClass.class)
    public void loginTest(String username, String password) {

        System.out.println("Username: " + username);
        System.out.println("Password: " + password);

        // Selenium code here
    }
}
```

---

# 🔥 How TestNG Executes

TestNG automatically runs:

```text id="26sj3p"
Iteration 1 → admin1 pass1
Iteration 2 → admin2 pass2
Iteration 3 → admin3 pass3
```

---

# ✅ Output

```text id="4a7b2r"
Username: admin1
Password: pass1

Username: admin2
Password: pass2

Username: admin3
Password: pass3
```

---

# 🔥 Internally How It Works

TestNG reads:

```java id="pl65zn"
Object[][]
```

like:

```java id="mq3n17"
{
 {"admin1","pass1"},
 {"admin2","pass2"},
 {"admin3","pass3"}
}
```

Each row = one test execution

---

# 🔥 Visual Flow

```text id="m9x0bo"
Excel row 1 → Test execution 1
Excel row 2 → Test execution 2
Excel row 3 → Test execution 3
```

---

# ✅ Real Selenium Usage Example

```java id="b6m0q0"
@Test(dataProvider = "loginData", dataProviderClass = DataProviderClass.class)
public void loginTest(String user, String pass) {

    driver.findElement(By.id("username")).sendKeys(user);
    driver.findElement(By.id("password")).sendKeys(pass);
    driver.findElement(By.id("login")).click();
}
```

---

# 🔥 Why Object[][] ?

Because:

```java id="t2z08m"
Object[row][column]
```

Example:

```java id="yxybgz"
Object[3][2]
```

means:

* 3 test runs
* 2 parameters each

---

# 🔥 Best Practices (Senior Level)

---

## ✔ Keep Excel logic separate

Do not write Excel code in test class

---

## ✔ Reusable utility

Use common ExcelReader class

---

## ✔ Handle null cells safely

```java id="n7mijw"
DataFormatter formatter = new DataFormatter();
```

Better than `.toString()`

---

## ✔ Cache workbook for performance

For large frameworks

---

# 🔥 Better Safe Code

```java id="d5wrqy"
DataFormatter formatter = new DataFormatter();
data[i - 1][j] = formatter.formatCellValue(sheet.getRow(i).getCell(j));
```

---

# 🔥 Common Mistakes

❌ Hardcoding file path
❌ Not closing workbook
❌ Wrong row indexing
❌ Header row included as data

---

# 🔥 Senior-Level Interview Answer

If interviewer asks:

### Why DataProvider with Excel?

Strong answer:

> *Using TestNG DataProvider with Excel supports data-driven testing by separating test logic from test data, making automation scalable and easier to maintain.*

---

# 🚀 One-Line Strong Answer

👉 *Excel data can be integrated with TestNG using Apache POI to read the file into Object[][], which is then supplied through a DataProvider so the same test executes with multiple datasets.*

---

# 💡 10-Year Experience Insight

At senior level:

> *I usually build a generic Excel utility that supports sheet name, row filtering, and dynamic column mapping so the framework remains maintainable as data grows.*

---

If you want next:
👉 I can explain **how to read only specific rows from Excel using DataProvider**
or
👉 **how to map Excel columns directly into HashMap<String,String> for advanced frameworks**.
